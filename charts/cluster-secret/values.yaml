# Default values for openshift-connector.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

appName:
token:

applicationClusters:
  - address:
    name:
    namespace:
